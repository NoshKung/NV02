#include <stdio.h>

int main() {
  int n, i,a;
  while (a != 0){

    printf("Enter a number: ");
    scanf("%d", &n);

    int fact = 1;
    for (i = 1; i <= n; i++){
      fact *= i;

    }
    printf("%d! = %d\n\n", n, fact);
    if(n == 0){break; }
  }
}
